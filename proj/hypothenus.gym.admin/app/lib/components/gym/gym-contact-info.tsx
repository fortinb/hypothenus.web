"use client"

import ContactInfo from "../contact/contact-info";

export default function GymContactInfo() {

    return (
       <ContactInfo index={0} id="gym_contact" formStatefield="contacts.0"/>
    );
}